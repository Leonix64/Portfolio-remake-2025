import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Portfolio-remake-2025-v4',
  webDir: 'www'
};

export default config;
